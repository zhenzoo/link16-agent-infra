# Deprecated Codex Entry Template

This file is retained only as a compatibility pointer. It is not copied into
any `CODEX_HOME`.

The maintained Codex entry template is:

`~/.claude-personal/skills/agent-profile-governance/references/AGENTS.codex.template.md`

Run `$agent-profile-governance` (or its `profile_governance.py docs --apply`
command) to render physical, profile-specific `AGENTS.md` files. Link16
`feishu/agent-profiles.json` is the only profile-to-home mapping.
