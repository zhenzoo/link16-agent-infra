---
doc_type: PLAN
doc_id: PLAN-931
title: 看门狗独立成 Link16 常驻部件（全机 agent 保活）
status: draft
purpose: 把 xhs-card-gen/_autopilot/watchdog.py 里「全机 agent 保活」那部分抽进 Link16，成为与 bridge_cron 同层同接口的常驻部件，并先建立这次推进的验收评分体系。
owns:
  - 本次迁移的目标、阶段、步骤与取舍
  - 通用职责与 xhs 专属职责的切分判据
  - 本次推进的 evaluation 评分体系定义
  - 每步的交付物与验证信号
does_not_own:
  - 看门狗落地后的运行时真源（迁移完成后归 ARCH-160）
  - wmux 面板驱动原语（见 ARCH-010）
  - 桥的收发与回传（见 ARCH-110）
  - xhs 写帖巡航本身的流程（见 xhs 仓 ARCH-310 / SOP-005）
  - 对外开放的脱敏闸（见 PLAN-926）
read_when:
  - 执行本次看门狗迁移的任何一步
  - 想知道某个职责该留 xhs 还是进 Link16
  - 要判断本次推进「算不算做完做好」
last_reviewed: 2026-08-19
---

# PLAN-931 · 看门狗独立成 Link16 常驻部件

> 立项 2026-08-19（align 四段走完 · 主人拍板）。
> **前置事实**：本 plan 由一次「黑窗排查」牵出——排查中发现看门狗放错仓、且有一个接口已静默失效两个月。

## 0. 立项背景与交付契约

### 0.1 为什么要动

`xhs-card-gen/_autopilot/watchdog.py`（691 行 / 31 函数 / 37 KB）其实是**两个东西被捆在一个文件里**：

1. **全机 agent 保活器**（通用）：扫全机所有 wmux workspace 的所有面板，发现 agent 卡在
   API error / 限流 / 断连且没在自愈 → 注一句「继续」把它推走。**这是 Link16 层的基础设施**，
   与写小红书帖子毫无关系。
2. **写帖巡航监工**（xhs 专属）：盯 xhs 内容目录的 mtime 判断整条线是否停滞、发布页铺好后
   发飞书 + 封面卡。

放错仓已经造成**真实腐烂**（非理论风险，2026-08-19 实测）：

| | 看门狗读的 | 桥实际写的 |
|---|---|---|
| bot 名册 | `xhs-card-gen/_autopilot/bridge-session-*.json`（7 个 · **冻结在 2026-06-27**） | `link16-agent-infra/feishu/_state/bridge-session-*.json`（21 个 · 活的） |
| arch 线 pty | `daemon-060b56bb`（陈旧） | `daemon-0f268f18`（真值） |

2026-06-27 桥从 xhs 迁入 Link16 那天，看门狗这个接口就断了。**它这两个月照常报警，但「是哪条 bot 线卡了」一直标错。**
这是本仓「尺子坏了但输出正常」的又一个实例：进程活着、日志在写、告警在发，唯独内容是错的。

### 0.2 主人拍板的五条约束（2026-08-19 · 不可推翻）

1. **形态**：不做飞书 skill，也**不是「模块 + CLI」两个东西**——**就是一个 Python 脚本**，
   形态与 `feishu/bridge_cron.py` 一模一样：**单文件 + `run/start/stop/status` 子命令**，起它就完了。
   （主人 2026-08-19 明确纠正：别搞复杂，原来什么形态就什么形态。）
2. **生命周期绑桥、不绑 xhs**：电脑开着 + 桥起了 → 看门狗必定在。xhs 没起，它只是不推写帖那条线，全机保活照常。
3. **xhs 侧留成瘦客户端**：职责2/3 留在 xhs，通用能力向 Link16 取。
   **发群消息那部分主人认为已不需要，但本次不动**——瘦身是独立一件事，不混做。
4. **中英文自动**：不让用户配置，**由系统检测该 session 上下文语言**，中文 session 发中文、英文 session 发英文。
5. **evaluation 是本次推进的评分体系**，不是给看门狗写回归测试。要建立「到底怎么样算做完、算做好」的机制，
   并用它判定每个 stage 的推进质量。

### 0.3 交付契约（量化 · 交付后按这张表一条条对账）

| 交付物 | 数量 | 具体是什么 |
|---|---|---|
| 新增运行时文件 | **1** | `feishu/bridge_watchdog.py` —— **就这一个脚本**，语言检测等全部并进去，不拆第二个文件 |
| 新增评分工具 | **1** | `tests/eval_plan931.py`（可跑出 0-100 分的验收评分器） |
| 新增测试文件 | **2** | `tests/test_bridge_watchdog.py`（功能）· `tests/test_bridge_watchdog_stress.py`（六类压力场景） |
| 新增配置文件 | **0**（硬指标） | 无缝接现有环境：一切从现有真源读，不新建 config |
| 新增文档 | **2** | 本 PLAN · `docs/ARCH-160-agent-watchdog.md`（迁移完成后的运行时真源） |
| 修改文件 | **5** | `feishu/feishu_bridge.py`（start/stop 挂钩 + 补 CREATE_NO_WINDOW）· `TOOLS.md` · `AGENTS.md` · `CHANGELOG.md` · xhs 的 `_autopilot/watchdog.py` |
| 退役项 | **1** | Windows 计划任务 `AutopilotWatchdog-Autostart` |
| 迁移函数 | **约 17 / 31** | 通用侧；精确切分表由 S2.1 产出 |
| 行为常量转配置 | **6** | 轮询 120s · 卡死 30min · 重报 30min · 确认 2 轮 · 冷却 600s · 心跳 15 轮 |
| 覆盖的守护对象 | **21 bot 会话 / 16 桥进程**（当前值） | 迁移前后必须一致 |
| CLI 动词 | **4** | `run / start / stop / status`（与 `bridge_cron.py` 完全一致） |

**上线状态**：全部 `N/A`（无前端）。

---

## S1 · 先建评分体系（evaluation harness） — [x] 2026-08-19 完成

> **为什么排第一**：align §4「能读数据的 infra 先造」。评分体系不先建，后面每一步都无法判定
> 「做好了没」，只能靠感觉——那正是主人这次点名要消灭的东西。

### S1.1 定义「做完 / 做好」的判据维度
1. **解决什么问题**：现在判断一步是否完成，靠的是我口头说「改完了」，主人无法独立核验。
2. **矛盾点**：判据太松 = 等于没有；太严 = 每步都卡住不能推进。且「做完」（功能在）与
   「做好」（没留债）是两件事，混成一个分数会互相掩盖。
3. **推荐方案**：**拆成两组分数**——`完成度`（功能性判据，二值：接口通没通、进程起没起、
   测试过没过）与 `质量度`（回归 / 净增 / 文档 / 可观测性，分档）。两组分别算分、都过线才算该 stage 完成。
   这样「功能跑通但留一堆债」不会被一个高分掩盖。
4. **改什么·改哪里**：本步只产出判据定义，写进本文件 S1.1 附表（不写代码）。
5. **交付物**：一张「维度 / 判据 / 数据从哪来 / 满分条件」表，覆盖至少 6 个维度。
6. **验证**：每个维度都能指出**具体的数据来源**（某个文件、某条命令的输出、某个测试的 exit code）；
   凡是只能靠人主观判断的维度，要么改写成可测形式，要么剔除。

#### 判据表 · 完成度（8 项 · 二值 · 必须全满才算「做完」）

| # | 维度 | 判据 | 数据从哪来 | 满分条件 |
|---|---|---|---|---|
| C1 | 部件能跑 | 脚本存在 + 语法过 + `status` 退出码 0 | 文件系统 · `py_compile` · exit code | 三者全真 |
| C2 | 接口对齐 | 它的子命令集合 == `bridge_cron.py` 的 `{run,start,stop,status}` | 两个文件的 AST 里 `cmd_*` 函数名 | 集合完全相等 |
| C3 | 名册接口正确 | 新脚本不含 `_autopilot` 路径 + Link16 名册命中活面板数 >0 且**严格优于** xhs 僵尸名册 | 两处名册 + `workspace.list` | 相对判据成立（见基线小节脚注 3：绝对命中率是永远达不到的摆设门槛） |
| C4 | 与桥同生命周期 | 桥的 `cmd_start`/`cmd_stop` 里各有一处带起/带停，且在 `not bot_filter` 分支 | `feishu_bridge.py` 源码扫描 | 两处都在且分支正确 |
| C5 | 单实例 | 连起两次后同名进程数 | 进程表 | == 1 |
| C6 | 测试通过 | `pytest tests/test_bridge_watchdog*.py` | exit code | == 0 |
| C7 | xhs 已瘦身 | xhs 侧不再含通用注入函数 | xhs `watchdog.py` 源码扫描 | 通用函数 0 命中 |
| C8 | 旧自启退役 | 计划任务 `AutopilotWatchdog-Autostart` | `schtasks` 查询 | 不存在或已 Disabled |

#### 判据表 · 质量度（8 项 · 每项 0/1/2 · 满分 16 · 达线 ≥13 且无 0 项）

| # | 维度 | 判据 | 数据从哪来 | 满分条件 |
|---|---|---|---|---|
| Q1 | 零新增配置 | 新增的本机配置文件数 | 文件清单 | == 0 |
| Q2 | 跨机安全 | 硬编码用户名 / 盘符 / python 绝对路径的命中数 | grep 新增文件 | == 0 |
| Q3 | 零黑窗 | 新增代码里起控制台程序的点带 `CREATE_NO_WINDOW` 的比例 | AST 审计 | 100% |
| Q4 | 压力覆盖 | 六类压力场景用例存在且通过 | `pytest -k stress` | 6/6 |
| Q5 | 测试自证 | 把防护改坏后对应用例是否变红（变异抽查） | 变异测试记录 | 抽查全中 |
| Q6 | 文档闭环 | ARCH-160 / TOOLS.md / AGENTS.md / CHANGELOG 四处登记 | 文件内容 grep | 4/4 |
| Q7 | 净减 ≥ 净加 | 两仓合并的代码行变化 | 两仓 `git diff --stat` | ≤ 0 |
| Q8 | 可观测性 | `status` 能报出「在看护几个面板 / 上次巡检时间 / 最近注入过谁」 | `status` 输出 | 三项齐全 |

> **为什么分两组**：`完成度` 是二值的——功能在不在，没有中间态；`质量度` 是分档的——债留多少。
> 混成一个总分，会出现「功能全跑通但一身债」被高分掩盖。两组分别过线，才叫做完**且**做好。

### S1.2 实现评分器 `tests/eval_plan931.py`
1. **解决什么问题**：判据写在文档里不会自己跑，主人还是得信我。
2. **矛盾点**：评分器本身可能坏（尺子坏了、输出仍正常）——它必须能自证。
3. **推荐方案**：评分器每个维度返回 `(分数, 证据字符串)`，**证据必须是实测输出的片段**
   （文件路径 + 行号、命令 exit code、进程 pid），不允许只返回分数。再加一个 `--self-test`：
   故意喂错误状态，验证该维度确实会扣分——**尺子先验尺子**。
4. **改什么·改哪里**：新增 `tests/eval_plan931.py`；不进 pytest 默认套件（它是验收工具、不是单测）。
5. **交付物**：可跑的评分器，输出「总分 + 逐维度分 + 每维度证据」。
6. **验证**：`--self-test` 全绿；对当前未改动的仓库跑一次，分数必须**明显偏低**（否则说明判据没咬合）。

### S1.3 打基线分（before）
1. **解决什么问题**：没有 before 就没有 after 可比，「改好了」无从证明。
2. **矛盾点**：基线要在动任何代码之前打，一旦开始改就再也拿不到了。
3. **推荐方案**：S2 起第一行代码之前跑一次，结果**写进本文件**（不只留在终端）。
4. **改什么·改哪里**：本文件新增「基线分」小节。
5. **交付物**：一组带日期的基线分数 + 证据。
6. **验证**：基线里必须包含那条已知坏掉的接口（bot 名册读错仓）被扣分——如果它没被扣分，说明判据漏了。

#### 基线分 · 2026-08-19 18:5x（动第一行迁移代码之前）

```
完成度 0/8   质量度 0/16   判定：做完=❌ 做好=❌
C1 部件能跑        ❌ 脚本不存在
C2 接口对齐        ❌ 子命令=无
C3 名册接口        ❌ 部件不存在，无从求值
C4 同生命周期      ❌ 桥的 cmd_start/cmd_stop 里没有带起/带停
C5 单实例          ❌ 常驻进程数 = 0
C6 功能测试        ❌ 测试文件不存在
C7 xhs 已瘦身      ❌ 仍留 read_all_panes_all_workspaces / nudge_pane / find_pane_error
C8 旧自启退役      ❌ AutopilotWatchdog-Autostart 仍在启用
Q1-Q8 全 0（部件、测试、文档均未开始；Q7 因两仓有未提交改动而无从判净增）
```

**建尺子过程中尺子自己被打脸两次（记下来，免得重蹈）**：

1. `--self-test` 抓出 **Q1 是摆设**——「没找到多余配置文件 = 满分」，可空目录里本来就什么都找不到。
   修法：任何判据都要**先确认被评对象存在**，否则判 0（「什么都没有」≠「做得好」）。
2. 首次跑基线时 **C3 报「workspace.list 返回不是 JSON」，但那明明是合法 JSON**——真因是评分器的
   `_run()` 把输出截断到 400 字再拿去解析。C3 那次判 0 虽然结论对，**理由是假的**。
   修法：`_run()` 绝不截断，截断只允许发生在展示层。
3. 基线还暴露 **C3 判据本身设计错**——「名册命中率必须 100%」永远达不到（16 条名册里只有 6 个 bot
   真在 wmux 面板里跑）。改成**相对判据**：Link16 名册命中活面板数 >0 且严格优于 xhs 僵尸名册。

> 这三条都是同一个教训的不同形状：**下结论前先验测量工具**。评分体系本身若不可信，
> 后面所有 stage 的「做完做好」都是空话。

---

## S2 · 切分与接口契约 — [ ]

### S2.1 逐函数切分表（通用 vs xhs 专属）
1. **解决什么问题**：31 个函数混在一个文件里，凭印象搬会漏或搬错。
2. **矛盾点**：有些函数**两边都用**（如 `rpc` / `log` / `read_pane`）——搬走 xhs 会断，留下 Link16 会缺。
3. **推荐方案**：分**三类**而不是两类：`通用`（搬）/ `xhs 专属`（留）/ `共用底座`（搬进 Link16，
   xhs 侧改为经 CLI 调用）。第三类是这次真正的设计活。
4. **改什么·改哪里**：本文件新增切分表，31 行、每行标类别 + 依据。
5. **交付物**：31 个函数的完整归类表。
6. **验证**：三类数量相加 = 31；每个「共用底座」函数都写明 xhs 侧改用什么方式取。

### S2.2 CLI 接口契约（对齐 bridge_cron）
1. **解决什么问题**：主人明确要求「同一层级、同一套接口」，不能又发明一套动词。
2. **矛盾点**：看门狗比 cron 多一个「查它现在在看护谁」的需求，硬塞进 4 个动词会别扭。
3. **推荐方案**：**主动词严格对齐** `run / start / stop / status`；额外信息塞进 `status --verbose`，
   **不新增顶层动词**。保持「学会一个就会另一个」。
4. **改什么·改哪里**：本文件写死契约表；S3.1 按它实现。
5. **交付物**：动词 / 参数 / 退出码 / 输出格式的对照表（与 `bridge_cron.py` 并列）。
6. **验证**：逐条与 `feishu/bridge_cron.py` 的实现比对，语义不一致处必须有书面理由。

### S2.3 状态文件与配置落点
1. **解决什么问题**：pid / log / 状态现在散在 xhs 的 `_autopilot/`，搬家后要有确定去处。
2. **矛盾点**：Link16 的 `feishu/_state/` 已有大量桥状态文件，混进去会难以分辨归属；
   另立目录又增加认知负担。
3. **推荐方案**：**沿用 `feishu/_state/` + 统一前缀 `watchdog-`**（与 `bridge-session-` 同款命名法），
   不新建目录。日志进 `feishu/_logs/watchdog.log`，与 `bridge-cron.log` 并列。
4. **改什么·改哪里**：本文件写死路径表。
5. **交付物**：状态文件 / 日志 / 配置的路径表 + 是否进 git 的标注。
6. **验证**：所有路径都能用「仓库根起的相对路径」唯一定位；无一硬编码盘符或用户名。

---

## S3 · 通用能力落 Link16 — [ ]

### S3.1 新建 `feishu/bridge_watchdog.py`
1. **解决什么问题**：通用保活能力还没有 Link16 侧的家。
2. **矛盾点**：整文件复制会把 xhs 的 691 行债一起搬进来；从零重写又可能丢掉那三道
   来之不易的防自激闸（注入文本不含错误词 / 有 retry 就不碰 / 连续静止 2 轮 + 10 分钟冷却）。
3. **推荐方案**：**按 S2.1 的表逐函数搬**，只搬「通用 + 共用底座」；三道闸**原样保留、一行不改**
   （它们是踩过 livelock 才有的），并在代码里注明出处。
4. **改什么·改哪里**：新增 `feishu/bridge_watchdog.py`。
5. **交付物**：可 `python feishu/bridge_watchdog.py run` 跑起来的常驻部件。
6. **验证**：起来后能读到的面板数 ≥ 当前 workspace 数；日志逐轮有输出；`py_compile` 过。

### S3.2 修好断了两个月的名册接口
1. **解决什么问题**：告警标注「哪条 bot 线」自 2026-06-27 起一直是错的。
2. **矛盾点**：xhs 那 7 个僵尸文件还在原地，如果新代码仍能读到它们，会掩盖问题。
3. **推荐方案**：新代码**只读 Link16 的 `feishu/_state/`**，并用 `bridge_env.py` 的现成路径解析
   （不自己拼路径）；同时**主动检测**「读到的 pty 是否存在于当前 wmux 拓扑」，全不匹配就
   **明确报错而不是静默降级**——静默降级正是当初没被发现的根因。
4. **改什么·改哪里**：`feishu/bridge_watchdog.py` 的 `load_bot_names` 等价实现。
5. **交付物**：能正确标注 bot 名的告警。
6. **验证**：拿当前真实 pty 比对，标注命中率 = 100%；再故意喂一份陈旧名册，验证它**报错**而不是默默标错。

### S3.3 通知走 Link16 自己的发送链
1. **解决什么问题**：现在调 xhs 的 `scripts/notify.py`，搬家后这条依赖必须断掉。
2. **矛盾点**：Link16 有多个发送入口（`notify.py` / `send_feishu_msg.py`），选错会绕开路由信封。
3. **推荐方案**：**告警走 `send_feishu_msg.py`**（带路由元数据信封，主人在 DM 收得到）；
   只有「桥自己死了」这类**桥不可用时的告警**才退回独立 webhook 通道——双通道冗余的意义就在这。
4. **改什么·改哪里**：`feishu/bridge_watchdog.py` 的通知函数。
5. **交付物**：两条通知路径 + 明确的选路规则。
6. **验证**：正常路径真发一条到主人 DM；再模拟桥挂，验证退回 webhook 仍能送达。

### S3.4 中英文自动检测
1. **解决什么问题**：注入语与告警文案现在写死中文，go public 后英文用户看不懂；但主人明确
   **不要让用户配置**。
2. **矛盾点**：「检测 session 语言」听起来简单，实际面板里混着代码、路径、英文报错——
   按字符统计极易误判成英文。
3. **推荐方案**：**只统计面板文本里的 CJK 字符占比**，且**先剔除代码块 / 路径 / URL** 再判；
   低于阈值判英文。阈值与剔除规则写进代码注释当合同。**判不准时默认中文**（当前唯一真实用户是中文）。
4. **改什么·改哪里**：`feishu/bridge_watchdog.py` **内部**一个纯函数 + 两套文案常量（**不拆第二个文件**）。
5. **交付物**：语言检测纯函数 + 中英两套文案。
6. **验证**：喂 6 个真实面板样本（3 中 3 英，含代码 / 路径干扰）全部判对；纯函数有单测。

### S3.5 单实例锁 / pid / 日志（对齐 cron）
1. **解决什么问题**：双开会导致同一面板被注两次「继续」。
2. **矛盾点**：xhs 版用 pid 文件判活，Link16 的 cron 用命令行扫进程——两套机制。
3. **推荐方案**：**跟 `bridge_cron.py` 一致**（顶替残留 + 单实例），不引第三套。
4. **改什么·改哪里**：`feishu/bridge_watchdog.py`。
5. **交付物**：单实例保证 + pid + 滚动日志。
6. **验证**：连起两次，第二次顶替第一次且只剩一个进程。

---

## S4 · 与桥同生命周期 — [ ]

### S4.1 挂进桥的 start / stop
1. **解决什么问题**：主人的语义是「桥起了 → 看门狗必定在」。
2. **矛盾点**：单 bot 起停（`start --bot X`）不该带起全局看门狗，否则每次单起都多一个进程。
3. **推荐方案**：**完全复刻 cron 的挂法**——只在 `not bot_filter`（整体 start/stop）时带起 / 带停。
4. **改什么·改哪里**：`feishu/feishu_bridge.py` 的 `cmd_start` / `cmd_stop`，紧挨现有 cron 那两处。
5. **交付物**：桥 start 自动起看门狗、stop 自动停。
6. **验证**：整体 start → 看门狗进程在；`start --bot X` → 不多起；整体 stop → 干净退出。

### S4.2 顺手补掉同类黑窗 bug
1. **解决什么问题**：桥带起 cron 的那两处 `subprocess.run` **也没带 `CREATE_NO_WINDOW`**，
   与 2026-08-19 修掉的看门狗黑窗是同一个 bug 类。
2. **矛盾点**：属于「顺手修」，但不修就等于新加的挂钩天生带同款缺陷。
3. **推荐方案**：**修**（原有两处 + 新加的挂钩处），因为本步正好在改这个函数；不扩大到无关文件。
4. **改什么·改哪里**：`feishu/feishu_bridge.py` 约 2397 与 2411 行附近。
5. **交付物**：桥 start / stop 全程零可见黑窗。
6. **验证**：用 2026-08-19 那套可见窗口探针，整体 start 一次，记录必须为零。

### S4.3 开机自启对齐
1. **解决什么问题**：主人要「开着电脑就在」。
2. **矛盾点**：现在是**两套自启**（桥有自己的、看门狗有 Windows 计划任务），会互相打架。
3. **推荐方案**：**并进桥那一套**（见 `PLAN-924-bridge-autostart-parity`），看门狗不再有独立自启。
4. **改什么·改哪里**：按 PLAN-924 的机制接入；本步不新造自启。
5. **交付物**：单一自启路径。
6. **验证**：重启机器（或模拟登录触发），桥起来后看门狗在。

### S4.4 退役 `AutopilotWatchdog-Autostart`
1. **解决什么问题**：旧计划任务会继续拉起 xhs 那个旧看门狗 → 双份保活、重复注入。
2. **矛盾点**：删早了会有一段空窗期没人保活。
3. **推荐方案**：**S4.3 验证通过之后才删**，且先 `Disable` 观察一轮再 `Unregister`（可回滚）。
4. **改什么·改哪里**：Windows 计划任务（机器状态、非仓库文件）；在本文件登记操作与回滚方法。
5. **交付物**：单一看门狗进程。
6. **验证**：机器上 watchdog 相关常驻进程数 = 1。

---

## S5 · xhs 侧改成瘦客户端 — [ ]

### S5.1 砍掉 xhs 侧的通用部分
1. **解决什么问题**：通用能力已在 Link16，xhs 再留一份 = 双份注入。
2. **矛盾点**：砍的过程中容易误伤职责 2/3 依赖的「共用底座」函数。
3. **推荐方案**：严格按 S2.1 的三类表砍；**共用底座改为经 Link16 CLI 调用**，不跨仓 import
   （跨仓 import 会把 xhs 绑死在 Link16 的目录结构上）。
4. **改什么·改哪里**：`xhs-card-gen/_autopilot/watchdog.py`。
5. **交付物**：只剩职责 2/3 的瘦看门狗。
6. **验证**：文件行数显著下降；`py_compile` 过；起来后不再注入任何面板。

### S5.2 明确「xhs 没起时的行为」
1. **解决什么问题**：主人的语义——xhs 没起，只是不推写帖线，全机保活照常。
2. **矛盾点**：xhs 侧瘦客户端起不起、由谁起，与 Link16 侧的生命周期不能纠缠。
3. **推荐方案**：xhs 瘦客户端**继续由 xhs 巡航总控管**（跟巡航同生死），Link16 侧**完全不感知它**。
4. **改什么·改哪里**：xhs 的 `SOP-005` / `ARCH-310` 对应段落；本文件登记契约。
5. **交付物**：两侧生命周期互不依赖的书面契约。
6. **验证**：停掉 xhs 全部进程，Link16 侧看门狗照常巡检并写日志。

### S5.3 xhs 回归
1. **解决什么问题**：不能因为搬家把写帖巡航搞坏。
2. **矛盾点**：完整跑一轮真巡航代价高、且会真发帖。
3. **推荐方案**：**分层验证**——用历史状态文件做离线回放，验证职责 2/3 的判断逻辑，不跑真巡航；
   只在最后由主人挑一个真实巡航窗口观察一轮。
4. **改什么·改哪里**：不改代码，新增一个离线回放检查。
5. **交付物**：回放验证结果。
6. **验证**：职责 2/3 在回放上的判断与迁移前一致。

---

## S6 · 测试 · 文档 · 公开准备 — [ ]

### S6.1 pytest 覆盖
1. **解决什么问题**：三道防自激闸是这套东西最危险的部分，改动后必须有回归网。
2. **矛盾点**：真起 agent 面板来测太重、且不确定。
3. **推荐方案**：**纯函数化后单测**——`find_pane_error` / 语言检测 / 冷却与确认轮次判断都做成
   无副作用纯函数，喂固定文本样本断言；面板 I/O 用假 RPC 打桩。
4. **改什么·改哪里**：新增 `tests/test_bridge_watchdog.py`。
5. **交付物**：至少覆盖「误判防护 / 三道闸 / 语言检测 / 名册解析」四组。
6. **验证**：pytest 全绿；故意把某道闸改坏，对应用例必须变红（**测试自证**）。

### S6.2 运行时真源 `ARCH-160`
1. **解决什么问题**：PLAN 是历史执行记录、不是真源；迁完必须有 ARCH 承接。
2. **矛盾点**：容易把 PLAN 内容整段复制成 ARCH，造成两处漂移。
3. **推荐方案**：ARCH **只写「现在是怎么运转的 + 为什么这么设计」**，取舍过程留在 PLAN，
   两者 `owns` 不重叠。
4. **改什么·改哪里**：新增 `docs/ARCH-160-agent-watchdog.md`（front matter 九字段齐全）。
5. **交付物**：运行时真源文档。
6. **验证**：`owns` / `does_not_own` 与 ARCH-010 / ARCH-110 / ARCH-150 无冲突。

### S6.3 索引与变更登记
1. **解决什么问题**：不进 `TOOLS.md` 和文档地图，未来 session 找不到 = 等于没有。
2. **矛盾点**：无。
3. **推荐方案**：`TOOLS.md` 加一行（场景 → 工具 → 怎么调）；`AGENTS.md` §6 文档地图加一行；
   `CHANGELOG.md` 记一条。
4. **改什么·改哪里**：`TOOLS.md` · `AGENTS.md` · `CHANGELOG.md`。
5. **交付物**：三处登记。
6. **验证**：从 `TOOLS.md` 出发，能在不读代码的情况下把看门狗起停查一遍。

### S6.4 go public 检查
1. **解决什么问题**：主人要求公开时能带上这个能力。
2. **矛盾点**：本 plan 不该把 PLAN-926 的脱敏闸整个吞下来。
3. **推荐方案**：**只对本次新增的文件做一次针对性检查**（无 open_id / 无主机名 / 无盘符 /
   无用户名 / 文案已中英双语），结果登记进 PLAN-926 的清单，不在这里重做脱敏闸。
4. **改什么·改哪里**：本文件登记检查结果；必要时在 PLAN-926 补一行。
5. **交付物**：针对性检查结论。
6. **验证**：对新增文件 grep 五类敏感模式，全零命中。

### S6.5 无缝接入现有环境与配置（主人硬要求 · 2026-08-19）
1. **解决什么问题**：主人要求「无缝接到现在的所有环境、所有配置里」——不能要求他改任何现有配置。
2. **矛盾点**：新部件天然想要自己的配置文件；但每多一份配置，就多一处会漂移、会忘记同步的地方。
3. **推荐方案**：**零新增配置文件**。要跑起来所需的一切都从**现有真源**读：
   bot 名册读 `bridge-bots.local.json` + `feishu/_state/`，profile 读 `agent-profiles.json`，
   路径解析走 `bridge_env.py`。行为常量给默认值 + 可选环境变量覆盖，**不新建 config 文件**。
4. **改什么·改哪里**：`feishu/bridge_watchdog.py` 的配置读取层。
5. **交付物**：一份「它读了哪些现有文件、新增了几个配置文件（应为 0）」的清单。
6. **验证**：在**未做任何配置改动**的机器上直接 `start` 就能跑起来并正确识别全部 bot；
   `git status` 里不出现任何新增的本机配置文件。

### S6.6 跨机适配（主人硬要求：所有电脑都适配）
1. **解决什么问题**：本仓是**两台机共享**的（见 `agent-registry.json` 的 `shared_repos`），
   代码在 A 机能跑、B 机跑不起来 = 没做完。
2. **矛盾点**：两台机的 Windows 用户名、盘符、Python 路径、wmux 版本、bot 名单**都可能不同**；
   而看门狗恰恰要读进程、读面板、读名册——全是机器相关的东西。
3. **推荐方案**：**全部走用户级 CLAUDE.md 的「跨机路径约定」**——home 相对 / `VIBECODING_ROOT` /
   `LINK16_AGENT_INFRA_ROOT`，**零硬编码用户名与盘符**；bot 名单**只从本机名册实时读**，不写死；
   `sys.executable` 取当前解释器不写死 python 路径。另加一条**自检子命令**，在目标机上一跑就报
   「我能不能找到 wmux / 名册 / 桥」。
4. **改什么·改哪里**：`feishu/bridge_watchdog.py`（路径层 + `status` 的自检输出）。
5. **交付物**：跨机自检能力 + 一份「本机适配检查」输出。
6. **验证**：① 对新增文件 grep 硬编码模式（`C:\Users\<名>`、`E:\`、写死 python 路径）零命中；
   ② **在另一台机上真跑一次** `status`，能正确列出那台机的 bot（这一条要主人配合或经 repo-sync 通知对面执行）。

### S6.7 压力测试（主人硬要求：各种压力测试都过关）
1. **解决什么问题**：这东西是**常驻**的，且**有权往别人的面板里注文字**——出错代价比一般脚本高得多。
   跑通一次不等于扛得住。
2. **矛盾点**：真实压力（几十个面板、RPC 超时、桥挂、wmux 重启）很难在开发时自然遇到；
   但等它在生产上遇到就已经出事了。
3. **推荐方案**：**造出来测**，六类各写一个场景：
   ① **规模**：假 RPC 返回 100+ 面板，验证单轮耗时可接受、不阻塞下一轮；
   ② **RPC 失败**：wmux 不通 / 超时 / 返回坏 JSON → 必须**跳过本轮**而不是崩溃或乱注入；
   ③ **桥挂**：桥进程消失 → 告警走 webhook，且**恢复后只报一次**；
   ④ **重复注入防护**：同一面板持续报错，验证 10 分钟冷却内**只注一次**（这是最危险的失效模式）；
   ⑤ **抖动**：面板文本在错误与正常间反复横跳，验证「连续静止 2 轮」的确认闸不被绕过；
   ⑥ **长跑**：连续跑够时长，验证内存/句柄不涨、日志不爆、pid 锁不失效。
4. **改什么·改哪里**：新增 `tests/test_bridge_watchdog_stress.py`。
5. **交付物**：六类压力场景的测试 + 一份结果表。
6. **验证**：六类全过；且**每一类都先证明它会失败**（把对应防护改坏 → 该用例变红），否则等于没测。

---

## S7 · 终审 — [ ]

### S7.1 评分器复跑（after vs before）
1. **解决什么问题**：证明「做完做好」不靠我说。
2. **矛盾点**：无。
3. **推荐方案**：跑 S1.2 的评分器，与 S1.3 的基线并排写进本文件。
4. **改什么·改哪里**：本文件新增「终审分」小节。
5. **交付物**：before / after 分数对照表 + 逐维度证据。
6. **验证**：完成度全部满分、质量度达线；任一维度未达线则该 stage 回退重做。

### S7.2 净减 ≥ 净加
1. **解决什么问题**：迁移最容易变成「新增一堆、旧的没删」。
2. **矛盾点**：Link16 侧必然净增，xhs 侧必然净减，分开看会各说各话。
3. **推荐方案**：**两仓合并计算**代码行与文件数，并单列「退役项」（旧计划任务、僵尸名册文件）。
4. **改什么·改哪里**：本文件新增增删账小节。
5. **交付物**：两仓合并的增删账。
6. **验证**：合计净增 ≤ 0，或有书面理由说明为何必须净增。

---

## 待办与回填

- [x] S1 评分体系 —— 判据表 16 维 + `tests/eval_plan931.py`（self-test 绿）+ 基线 0/8 · 0/16
- [ ] S2 切分与接口契约
- [ ] S3 通用能力落 Link16
- [ ] S4 与桥同生命周期
- [ ] S5 xhs 瘦客户端
- [ ] S6 测试 · 文档 · 公开准备
- [ ] S7 终审

### 执行日志（内联回填 · 只追加）

- **2026-08-19 立项。** 前置修复已完成（不属本 plan 的 stage，登记备查）：
  xhs `_autopilot/watchdog.py` 三处 `subprocess` 补 `CREATE_NO_WINDOW`，
  根治「每约 127 秒弹一次可见控制台窗口」；重启后由可见窗口探针验证 7 分钟零闪窗，
  同期日志 `18:06:50 飞书桥在线 · 开始看护` 证明被修函数仍正常工作。
