---
doc_type: PLAN
doc_id: PLAN-1060
title: 桥进程与告警可靠性修复及跨机更新
status: active
purpose: 完成三机复核发现的进程观测、停止、单实例与告警缺口，并交付各机运行验收。
owns:
  - 本次修复的执行状态与验收证据
  - 本次发布和各机更新回执
does_not_own:
  - 长期运行合同（见 ARCH-110、ARCH-160）
  - 用户级沟通规则
read_when:
  - 继续本次修复或核对跨机更新情况
last_reviewed: 2026-09-06
---

# 桥进程与告警可靠性修复及跨机更新

## 目标、授权与基线

用户已授权完成修复、commit、push，并通知所有机器拉取更新。更新包括受影响的桥、cron、watchdog；不关闭 agent 会话或 wmux 面板，不覆盖各机无关 dirty 修改。

基线为 main 90f71ec，TUF19 有 Claude 留下的三份未提交补丁，纳入本次完整修复。独立回归为 535 passed + 37 subtests；原代码运行新增九项为 7 failed / 2 passed。TUF19/TB24/TB25 的 R6 NameError 分别累计 3111/3023/3672 次（不同采样时间，非恒定计数）。TB26 尚未核验，不能断言已实际发作。

已复现：PowerShell rc=0/stderr非空仍被误判空表；整体 stop 先停守护再查询桥；未知时启动没有排他保证；R4/R6 发送失败仍推进已告警状态。进程快照、状态和守护查询须一起修正，避免同一错误换入口复发。

## 方案与验收

复用 bridge_injection.ProcessFileLock 的操作系统文件锁；服务持有到退出，控制命令另持事务锁。进程查询统一为带成功标记及合法 schema 的快照，PowerShell 使用 ErrorAction Stop；失败为 unknown，禁止转为空表。run 不杀其他服务实例，start 在事务锁内进行已知旧实例的受控替换。查询未知不停止、不启动；停止等待目标进程退出后才报告成功。整体停止先验证所有查询，再停桥，最后停守护。

R4 仅在确认通知成功后置位。R6 仅在确认通知成功后推进 seen，通知冷却只记录成功；写状态时保留通知函数更新的字段。心跳暴露巡检失败，进程活不能代替功能健康。

验收包含三种独立路径：失败/schema 注入，真实多进程互斥及异常退出释放，真实 PowerShell 查询与无害进程停止；再运行全仓回归。故障通知全部隔离，不向主人发假告警。生产更新先本机后其他机器，确认版本、服务唯一性、新鲜心跳、R6 不再新增及真实回信。

## 当前计划

✅ Stage 1｜修复 Link16 看门狗巡检报错、Windows 查询误判和桥重复启动（实际 03:10）

　✅ 1.1 严格进程快照、生命周期锁和停止确认接入三种服务（实际 03:06）

　✅ 1.2 真实系统探针与告警重试通过；全仓 562 passed + 37 subtests（实际 03:10）

🔄 Stage 2｜提交推送 Link16 修复，刷新 TUF19 桥、cron 与看门狗（ETA 03:18）

　🔄 2.1 审阅提交并推送 main（ETA 03:14）

　⏳ 2.2 逐项刷新受影响服务，确认唯一进程和新巡检心跳（ETA 03:18）

✅ Stage 3｜将计划标题的具体对象要求同步到本机 Claude/Codex（实际 03:06）

⏳ Stage 4｜通知 TB24、TB25、TB26 更新并回报巡检和服务状态（首批 ETA 03:25；全部完成依赖各机响应）

## 观测与交付账本

- 源码与隔离测试：562 passed + 37 subtests，18.83 秒；真实 PowerShell 错误和进程退出、两个独立进程争锁及崩溃释放、真实四轮 R4/R6 告警重试均通过。两条现有飞书 SDK deprecation warning。
- 测试修正：首次新增冷却测试只替换 STATE_DIR、遗漏 ALERTS_PATH，误写本机一条 unit:hwm_corrupt 测试时间戳；已按确切键移除且保留其他状态，补齐路径隔离后全仓通过；未发送真实测试告警。
- 用户级沟通：共享源 ~/.claude-personal/CLAUDE.md 已增加具体对象要求，renderer 写五份派生入口，六个账号均 source/ok，再次 apply 零写入。完整 doctor 因既有 cck launch.sh 缺失非零，未声明环境全绿。
- 飞书可见性：02:30—03:01 出站无新活动；03:03 独立文字补发并通过读取接口回读（om_x100b66f23af834a4c3858da07bbef12）。卡片 edit receipt 为成功，但 GET 仅返回占位，客户端实际显示未知。
- 本机生产：待更新；保留当前 agent 会话。
- TB24：待发布后通知。
- TB25：待发布后通知，明确保留 trust 启动修复等 dirty。
- TB26：待发布后通知核验，离线则记录最后投递和恢复信号。
- 发布版本、commit 与远端：待验收后记录。

## 已否决与回退

- 02:28 将总 ETA 从 03:05 改为 03:25：补齐三种服务共用查询与启动资格确认。03:01 恢复工作时处理用户反馈的长时间静默，重排为四阶段；原 Stage 1 ETA 02:30，实际 03:10，延后 40 分钟，包含进程控制扩展、出站调查及测试隔离修正。

- 02:05 用户指出计划标题缺具体对象；加入 Stage 4 沟通母版同步，原跨机阶段移为 Stage 5，修复和发布目标保持。总 ETA 由 03:00 调整为 03:05。

- 否决“查不了仍允许新桥上线”：延长超时不能提供排他性。
- 否决“测试全绿即稳定”：新增用例必须覆盖原遗漏的真实分支与并发时序。
- 回退使用明确版本的源码恢复并按同样服务停启流程验收；不 reset dirty、不关闭会话、不 force push。
